# =============================================================================
# CM.HFT — Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
#   cp .env.example .env
#
# NEVER commit .env to version control — it contains secrets.
# =============================================================================

# ---------------------------------------------------------------------------
# Grafana
# ---------------------------------------------------------------------------
GF_SECURITY_ADMIN_PASSWORD=CHANGEME_grafana_admin_password

# ---------------------------------------------------------------------------
# Alerting Integrations
# ---------------------------------------------------------------------------
PAGERDUTY_SERVICE_KEY=CHANGEME_pagerduty_service_key
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/CHANGEME/CHANGEME/CHANGEME

# ---------------------------------------------------------------------------
# AWS Credentials (for S3 backups from within containers)
# ---------------------------------------------------------------------------
# Prefer IAM instance profiles in production. Only set these for local dev
# or environments without instance metadata access.
AWS_ACCESS_KEY_ID=CHANGEME_aws_access_key
AWS_SECRET_ACCESS_KEY=CHANGEME_aws_secret_key
AWS_DEFAULT_REGION=ap-northeast-1
